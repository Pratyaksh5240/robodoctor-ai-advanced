/**
 * @deprecated RoboDoctor AI has migrated completely to MongoDB & Mongoose.
 * Auth is now managed via @/lib/auth and database via Next.js MongoDB API routes.
 */
export { auth } from "@/lib/auth";
export const db = null as any;
